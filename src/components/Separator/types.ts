export type SeparatorProps = {
  spaceBlockStart?: string;
  spaceBlockEnd?: string;
  className?: string;
};

export type ContainerProps = {
  $spaceBlockStart?: string;
  $spaceBlockEnd?: string;
};
